[project]
name = "exordos_core"
description = "Exordos Core services"
readme = "README.md"
authors = [{name = "Genesis Corporation", email = "eugene@frolov.net.ru"}]
dynamic = ["version"]
license = {text = "Apache-2.0"}
requires-python = ">=3.10"
classifiers = [
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
]
dependencies = [
    "oslo.config>=3.22.2,<10.0.0",  # Apache-2.0
    "bjoern>=3.2.2",  # BSD License (BSD-3-Clause)
    "gcl_looper>=1.2.3,<2.0.0",  # Apache-2.0
    # The floor is the version the committed OpenAPI specs are generated
    # with. 15.3.0 emits component `$ref`s where 15.2.x inlined the same
    # schemas, so a lower floor means whoever regenerates decides what the
    # spec looks like, and the diff is tens of thousands of lines either way.
    "restalchemy>=15.3.0,<16.0.0",  # Apache-2.0
    "Authlib>=1.3.2,<2.0.0",  # BSD License (BSD-3-Clause)
    "bazooka>=1.3.0,<2.0.0",  # Apache-2.0
    "Jinja2>=3.1.5,<4.0.0",  # BSD License (BSD-3-Clause)
    "izulu>=0.50.0,<1.0.0",  # MIT License
    "gcl_iam>=1.3.2,<2.0.0",  # Apache-2.0
    "gcl_sdk[libvirt]>=3.3.2,<4.0.0",  # Apache-2.0
    "gcl_certbot_plugin>=0.0.12,<1.0.0",  # Apache-2.0
    "pyotp>=2.9.0,<3.0.0",  # MIT License
    "pyyaml>=6.0.0,<7.0.0",  # MIT
    "netaddr>=1.3.0,<2.0.0",  # BSD License (BSD License)
    "cryptography>=45.0.5,<47.0.0",  # BSD-3
    "firebase-admin>=6.0.0,<8.0.0",  # Apache-2.0
    "altcha>=0.2.0,<2.2.0",  # MIT License
    "openapi-schema-validator>=0.8.1",  # BSD-3
    "packaging>=26.0,<=26.3",  # Apache-2.0 or BSD
]
[project.urls]
homepage = "https://github.com/infraguys/exordos_core/"

[build-system]
requires = [
    "setuptools>=75.3.3",  # last version for python3.8
    "setuptools_scm>=10.0.5"
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]


[project.optional-dependencies]
dev = [
    "tox>=4.0.0",
    "tox-uv",
    "pytest>=7.0.0",
]
test = [
    "coverage>=4.0",
    "mock>=3.0.5,<6.0.0",  # BSD
    "pytest>=8.0.0,<10.0.0",  # MIT License (MIT)
    "pytest-xdist[psutil]>=3.6.1,<4.0.0",  # MIT License (MIT)
    "pytest-timer>=1.0.0,<2.0.0",  # MIT License (MIT)
    "dnspython>=2.6.0,<3.0.0",  # ISC License
    "ruamel-yaml==0.19.1",
]
mypy = [
    "mypy",
]
ruff = [
    "ruff",
]
shellcheck = [
    "shellcheck-py",
]
docs = [
    "zensical>=0.0.51",
    "mkdocs-glightbox",
    "mkdocs-render-swagger-plugin",
    "mkdocs-static-i18n",
    "ruamel-yaml==0.19.1",  # writes the OpenAPI documents the site serves
]

[tool.ruff]
exclude = [
    ".bzr",
    ".direnv",
    ".eggs",
    ".git",
    ".git-rewrite",
    ".hg",
    ".ipynb_checkpoints",
    ".mypy_cache",
    ".nox",
    ".pants.d",
    ".pyenv",
    ".pytest_cache",
    ".pytype",
    ".ruff_cache",
    ".svn",
    ".tox",
    ".venv",
    ".vscode",
    "__pypackages__",
    "_build",
    "buck-out",
    "build",
    "dist",
    "node_modules",
    "site-packages",
    "venv",
    "docs",
]

# Same as Black.
line-length = 88
indent-width = 4
# Assume Python 3.10
target-version = "py310"
[lint]
# Enable Pyflakes (`F`) and a subset of the pycodestyle (`E`)  codes by default.
select = ["E4", "E7", "E9", "F"]
ignore = []
# Allow fix for all enabled rules (when `--fix`) is provided.
fixable = ["ALL"]
unfixable = []
# Allow unused variables when underscore-prefixed.
dummy-variable-rgx = "^(_+|(_+[a-zA-Z0-9_]*[a-zA-Z0-9]+?))$"
[format]
# Like Black, use double quotes for strings.
quote-style = "double"
# Like Black, indent with spaces, rather than tabs.
indent-style = "space"
# Like Black, respect magic trailing commas.
skip-magic-trailing-comma = false
# Like Black, automatically detect the appropriate line ending.
line-ending = "auto"

[tool.ruff.lint.isort]
known-first-party = ["exordos_core"]
combine-as-imports = false
force-sort-within-sections = true
order-by-type = true
split-on-trailing-comma = true
force-single-line = true

[tool.setuptools.packages.find]
where = ["."]

[project.scripts]
ec-user-api = "exordos_core.cmd.user_api:main"
ec-orch-api = "exordos_core.cmd.orch_api:main"
ec-boot-api = "exordos_core.cmd.boot_api:main"
ec-status-api = "exordos_core.cmd.status_api:main"
ec-gservice = "exordos_core.cmd.gservice:main"
ec-core-agent = "exordos_core.cmd.core_agent:main"
ec-bootstrap = "exordos_core.cmd.bootstrap:main"
ec-bootstrap-templates = "exordos_core.cmd.bootstrap_templates:main"
ec-openapi-spec = "exordos_core.cmd.openapi_spec:main"
ec-em-api-docs = "exordos_core.cmd.em_api_docs:main"
ec-manifest-schema = "exordos_core.cmd.manifest_schema:main"
exordos-repo-proxy-gservice = "exordos_core.cmd.repo_proxy_gservice:main"

[project.entry-points."gcn_network_driver"]
DummyNetworkDriver = "exordos_core.network.driver.base:DummyNetworkDriver"
FlatBridgeNetworkDriver = "exordos_core.network.driver.flat:FlatBridgeNetworkDriver"

[project.entry-points."exordos.repo_proxy.drivers"]
NginxProxyRepoDriver = "exordos_core.repo.drivers.nginx:NginxProxyRepoDriver"
BootstrapProxyRepoDriver = "exordos_core.repo.drivers.bootstrap:BootstrapProxyRepoDriver"
DummyMigrationRepoDriver = "exordos_core.repo.drivers.dummy_migration:DummyMigrationRepoDriver"
DatabaseProxyRepoDriver = "exordos_core.repo.drivers.database:DatabaseProxyRepoDriver"

[project.entry-points."gcl_sdk_event_payloads"]
IamUserRegistration = "exordos_core.events.payloads:RegistrationEventPayload"
IamUserResetPassword = "exordos_core.events.payloads:ResetPasswordEventPayload"

[project.entry-points."gcl_sdk_universal_agent"]
PasswordCapabilityDriver = "exordos_core.agent.universal.drivers.secret.password:PasswordCapabilityDriver"
CoreDNSCertificateCapabilityDriver = "exordos_core.agent.universal.drivers.secret.cert:CoreDNSCertificateCapabilityDriver"

[tool.uv]
package = true

[tool.mypy]
exclude = [
    '.venv',
    'exordos_core/tests',
]
python_version = "3.12"
disallow_untyped_defs = true
show_error_codes = true
no_implicit_optional = true
warn_return_any = false
warn_unused_ignores = true
ignore_missing_imports = true

[tool.setuptools.package-data]
exordos_core = ["*.yaml", "*.md"]

[tool.coverage.run]
branch = true
source = [
    "exordos_core"
]
command_line = "-m pytest -s exordos_core/tests/functional"
omit = [
    "exordos_core/tests/*",
    ".venv",
    "memory:0x"
]

[tool.coverage.report]
# Regexes for lines to exclude from consideration
exclude_also = [
    # Don't complain about missing debug-only code:
    "def __repr__",
    "if self\\.debug",
    "if settings\\.DEBUG",

    # Don't complain if tests don't hit defensive assertion code:
    "raise AssertionError",
    "raise NotImplementedError",

    # Don't complain if non-runnable code isn't run:
    "if 0:",
    "if __name__ == .__main__.:",

    "if TYPE_CHECKING:",

    # Don't complain about abstract methods, they aren't run:
    "@(abc\\.)?abstractmethod",
    ]

ignore_errors = true

[tool.coverage.html]
directory = "cover"

[tool.coverage.xml]
output = "cover/coverage.xml"
