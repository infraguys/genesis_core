[DEFAULT]
verbose = True
debug = True

[db]
connection_url = postgresql://genesis_core:genesis_core@127.0.0.1:5432/genesis_core
connection_pool_size = 2

[metrics]
enabled = False

[logging]
config = logging.yaml

[user_api]
bind_host = 0.0.0.0
bind_port = 11010

[orch_api]
bind_host = 0.0.0.0
bind_port = 11011

[status_api]
bind_host = 0.0.0.0
bind_port = 11012

[events]
# enable = False
# client_type = async
# event_type_mapping_filepath = event_type_mapping.yaml
# genesis_notification_endpoint = http://127.0.0.1:8080/
# genesis_api_version = v1
# genesis_api_token = "<inser token here>"

[iam]
# captcha_required_default = False
# captcha_salt =
# captcha_key =
# captcha_expires = 10
# captcha_max_number = 100000
